/**
 * Guxo Flights' brand values for the app's own static styles. They come
 * straight from the shared design system so there is one source of truth.
 */

import '@/global.css';

import { brands, maxContentWidth, radius, spacing } from '@/design-system/tokens';

export const Colors = brands.guxoFlights.colors;
export const Spacing = spacing;
export const Radius = radius;
export const MaxContentWidth = maxContentWidth;
